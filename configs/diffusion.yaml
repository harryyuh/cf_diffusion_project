seed: 42
device: cuda
# Base directory; checkpoints/logs go under:
#   {output_dir}/with_vae_condition/   when use_vae_condition: true
#   {output_dir}/parent_only/          when use_vae_condition: false
# Optional override: checkpoint_subdir: my_run  -> {output_dir}/my_run/
output_dir: /scratch/gilbreth/yu1331/ckpts/cf_diffusion
data_root: /scratch/gilbreth/yu1331/datasets/global
father_key: thickness
train_split: train
data_mode: images_csv
batch_size: 64
num_workers: 0
# If false: train p(x|A) with parent A only (cond_dim=1); vae_checkpoint is unused.
use_vae_condition: false
vae_checkpoint: /scratch/gilbreth/yu1331/ckpts/cf_vae/checkpoints/vae_best.pt
vae_config: configs/vae.yaml
in_channels: 1
image_size: 28
timesteps: 1000
beta_start: 0.0001
beta_end: 0.02
unet_base_channels: 32
unet_channel_mults:
- 1
- 2
- 4
time_emb_dim: 128
epochs: 100
lr: 0.0001
ddim_eta: 0.0
