# Conditional diffusion decoder training
seed: 42
device: cuda
output_dir: outputs/diffusion
data_root: data/morphomnist
train_split: train
data_mode: images_csv
batch_size: 64
num_workers: 0
vae_checkpoint: outputs/vae/checkpoints/vae_best.pt
vae_latent_dim: 32
vae_hidden_dims: [32, 64, 128]
latent_analysis_dir: outputs/logistic
in_channels: 1
image_size: 28
timesteps: 1000
beta_start: 0.0001
beta_end: 0.02
cond_mlp_hidden_dim: 128
cond_emb_dim: 128
cond_mlp_dropout: 0.0
unet_base_channels: 32
unet_channel_mults: [1, 2, 4]
time_emb_dim: 128
epochs: 100
lr: 0.0001
